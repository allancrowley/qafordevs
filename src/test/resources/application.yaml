
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/qafordevs_test
    hikari:
      username: postgres
      password: postgres
      driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true